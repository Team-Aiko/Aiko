import { Injectable } from '@nestjs/common';

@Injectable()
export default class ChatService {
    async createGroupChatRoom() {
        try {
        } catch (err) {
            throw err;
        }
    }
}
